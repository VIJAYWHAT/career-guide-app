# Serve the built app (required). Do NOT serve the repo root or you will see 404 for /src/main.tsx
[build]
  command = "npm run build"
  publish = "dist"

# SPA fallback for client-side routing when served from a subpath
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
